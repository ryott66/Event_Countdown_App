# Event Countdown - Codex Instructions

このプロジェクトで Codex に作業を依頼するときの常設ルール。会話冒頭で必ず読み込まれるので、明示的に「ルール変更」と言わない限り、以下を必ず守る。

## ブランチ戦略（ハイブリッド運用）

- **通常は `feat/...` / `fix/...` / `chore/...` ブランチを main から切って main に直接 PR する**
- 大規模変更（DB スキーマ変更を伴う / 3 ファイル以上の構造変更 / 複数機能をまとめて出したい）の場合のみ develop を経由する：
  1. develop を最新の main から fresh-cut（または fast-forward 更新）
  2. その develop から派生ブランチ群を切る
  3. 個別 PR を develop にマージ
  4. 最後に develop → main を 1 本の PR でマージ
- main へ強制 push しない、main / develop でいきなり編集を始めない

## ブランチ確認ルール

ユーザーから機能実装の依頼を受けたら、**最初の編集の前に必ず**：

1. `git branch --show-current` で現在のブランチを確認
2. `feat/...` `fix/...` `chore/...` で今回のタスクに合っていればそのまま着手
3. `main` / `develop` / 別タスクのブランチにいる場合、または「次これ」のように連続で別タスクが来た場合は「今 X ブランチですが新しい feat ブランチ切りますか？」と一度確認する

会話の流れだけで「同じブランチで作業中だろう」と思い込まないこと。前タスクの PR マージ後に main に戻されている可能性があるため。

## PR フォーマット

PR 本文は次の構造で作る：

```markdown
### Codex作成

## 概要
1〜2 文で何をしたかを要約。

## 背景
なぜやったかの説明。issue 番号があれば言及する。

## 変更内容

### サブセクション見出しで分類
* 箇条書きで列挙
* 太字で **ハイライト** を入れる
* インラインコード `xxx` を活用

## 動作確認

- [ ] チェックボックス形式で確認項目を列挙
- [ ] 既存挙動の維持 / 新機能の動作 / レスポンシブ など

Closes #N
```

タイトルは日本語で簡潔に（プレフィックス不要、PR 一覧で内容が分かるように）。

## コミットメッセージ

- 日本語で書く
- 種別プレフィックスを付ける: `feat:` `fix:` `docs:` `style:` `refactor:` `chore:`
- 例: `feat: イベントにヒーロー画像のアップロード機能を追加`
- 例: `fix: タイトル入力のplaceholderを「誕生日」に変更`
- 例: `style: イベント詳細ページのメモをお手紙カード風に刷新`

## 応答言語

日本語で応答する（コード内コメントは英語/日本語どちらでも、既存ファイルの傾向に合わせる）。
